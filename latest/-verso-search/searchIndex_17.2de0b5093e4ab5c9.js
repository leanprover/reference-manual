window.docContents[17].resolve({"/Build-Tools-and-Distribution/Lake/#lake-config-toml":{"contents":"TOMLTom's Obvious Minimal Language is a standardized format for configuration files. configuration files describe the most-used, declarative subset of Lake package configuration files.\nTOML files denote tables, which map keys to values.\nValues may consist of strings, numbers, arrays of values, or further tables.\nBecause TOML allows considerable flexibility in file structure, this reference documents the values that are expected rather than the specific syntax used to produce them.\n\nThe contents of lakefile.toml should denote a TOML table that describes a Lean package.\nThis configuration consists of both scalar fields that describe the entire package, as well as the following fields that contain arrays of further tables:\n\n* require* lean_lib* lean_exe\n\nFields that are not part of the configuration tables described here are presently ignored.\nTo reduce the risk of typos, this is likely to change in the future.\nField names not used by Lake should not be used to store metadata to be processed by other tools.\n\n\n\n\n\n\n\n\n\n","context":"Lean Reference\u0009Build Tools\u0009Lake\u0009Configuration File Format","header":"24.1.3.1. Declarative TOML Format","id":"/Build-Tools-and-Distribution/Lake/#lake-config-toml"},"/IO/Async/#async-select":{"contents":"Event selection involves both selectors, which are the source of events, and selectables, which pair selectors with code to be executed when the selector's event occurs.\nWhen a selector's event occurs, the selector has resolved.\nA selectable's code is not executed immediately when its selector resolves; instead, it is run when invoked by event selection.\nWhen a selectable whose selector has resolved is chosen for execution, it is selected.A Selector α provides a value of type α when its event occurs, while a Selectable α contains an Async action to run when its selector has resolved.\nThe type of the selector in a Selectable is a field of the constructor Selectable.case, rather than a parameter to the type; this means that selectables that are waiting on different types of event data can be used together.\n\nAn event source that can be multiplexed using Selectable.one, see the documentation of\nSelectable.one for how the protocol of communicating with a Selector works.Attempts to retrieve a piece of data from the event source in a non-blocking fashion, returning\nsome if data is available and none otherwise.Registers a Waiter with the event source. Once data is available, the event source should\nattempt to call Waiter.race and resolve the Waiter's promise if it wins. It is crucial that\ndata is never actually consumed from the event source unless Waiter.race wins in order to\nprevent data loss.A cleanup function that is called once any Selector has won the Selectable.one race.\n\nAn event source together with a continuation to call on data obtained from that event source,\nusually used together in conjunction with Selectable.one.The event source.The continuation that is called on results from the event source.\n\nEvent selection is invoked using three operators:\n\n* Selectable.one blocks until one selectable's event occurs and returns the resulting value,* Selectable.tryOne checks whether any selectable is resolved and returns the associated value but does not block,* Selectable.combine creates a new Selector whose event occurs when any of the underlying Selectables selector's event occurs, yielding the Selectable's data.\n\nPerforms fair and data-loss free multiplexing on the Selectables in selectables.selectables is combined into a single Selector via Selectable.combine:1. If Selector.tryFn immediately yields a value, the corresponding Selectable.cont is\nexecuted and its result is returned immediately.2. Otherwise, a single Waiter is registered with the combined Selector using\nSelector.registerFn. Once the Waiter is resolved, Selector.unregisterFn is called\nunconditionally, cleaning up every entry of selectables, before the result (or error) is\nreturned to the caller. This holds no matter how the race is decided: a winning result, an\nerror raised while resolving the Waiter, or a registerFn that throws mid-registration.\n\nPerforms fair and data-loss free non-blocking multiplexing on the Selectables in selectables.This function only tries the non-blocking tryFn for each Selectable without registering\nwaiters or blocking. It returns some result if any Selectable is immediately available,\nor none if all would block.The protocol for this is as follows:1. The selectables are shuffled randomly for fairness.2. Run Selector.tryFn for each element in selectables. If any succeed, the corresponding\nSelectable.cont is executed and its result is returned as some result.3. If none succeed, none is returned immediately without blocking.\n\nCreates a Selector that performs fair and data-loss free multiplexing on multiple Selectables.\nThis allows the multiplexing operation to be composed with other selectors.The returned Selector polls and registers the selectables in random order for fairness. If\nregistering with one of them fails, the error is reported through the Waiter unless the race\nwas already won, in which case the winner's result takes priority.\n\nPolling Without BlockingSelectable.tryOne checks whether any selector has already resolved and returns the corresponding value immediately, or none if none has, rather than blocking.\nDefining selection with := rather than ← makes pick the Async computation itself rather than its result, so the same poll can be run more than once.#eval show IO (Option String × Option String × Option String) from do\n  let colors ← Channel.new (α := String)\n  let flavors ← Channel.new (α := String)\n  let pick := Selectable.tryOne #[\n    .case colors.recvSelector fun color => return color,\n    .case flavors.recvSelector fun flavor => return flavor\n  ]\n  let whenEmpty ← pick.block\n  discard <| colors.send \"gray\"\n  let afterColor ← pick.block\n  discard <| flavors.send \"salty\"\n  let afterFlavor ← pick.block\n  return (whenEmpty, afterColor, afterFlavor)\n(none, some \"gray\", some \"salty\")\n\n\nSelection and TimeoutsA CloseableChannel provides a selector via CloseableChannel.recvSelector that resolves when the channel receives a value.\nSelector.sleep is a selector that resolves after the specified number of milliseconds have passed.\nThe function recv combines these, waiting for up to 100 milliseconds to receive a value, after which it terminates without one:def recv (ch : CloseableChannel Nat) : Async (Option Nat) := do\n  Selectable.one #[\n    .case ch.recvSelector fun n? => return n?,\n    .case (← Selector.sleep 100) fun () => return none\n  ]\nIf the channel contains a value, then the recvSelector wins:#eval show IO _ from do\n  let ch ← CloseableChannel.new (α := Nat)\n  discard <| ch.send 42\n  (recv ch).block\nsome 42\nIf not, the timer wins:#eval show IO _ from do\n  let ch ← CloseableChannel.new (α := Nat)\n  -- nothing sent: the timeout wins\n  (recv ch).block\nnone\n\n\nSelectionA CloseableChannel provides a selector via CloseableChannel.recvSelector that resolves when the channel receives a value.\nThe function recv2 selects the first value returned on either channel:def recv2 (ch1 ch2 : CloseableChannel Nat) : Async (Option Nat) := do\n  Selectable.one #[\n    .case ch1.recvSelector fun n? => return n?,\n    .case ch2.recvSelector fun n? => return n?\n  ]\nIf only one channel contains a value, then it is returned:#eval show IO _ from do\n  let ch1 ← CloseableChannel.new (α := Nat)\n  let ch2 ← CloseableChannel.new (α := Nat)\n  discard <| ch1.send 1\n  (recv2 ch1 ch2).block\nsome 1\n#eval show IO _ from do\n  let ch1 ← CloseableChannel.new (α := Nat)\n  let ch2 ← CloseableChannel.new (α := Nat)\n  discard <| ch2.send 2\n  (recv2 ch1 ch2).block\nsome 2\nIf neither channel contains a value, then recv2 blocks until one does; the first one to have a value wins:#eval show IO _ from do\n  let ch1 ← CloseableChannel.new (α := Nat)\n  let ch2 ← CloseableChannel.new (α := Nat)\n  discard <| IO.asTask (prio := .dedicated) do\n    IO.sleep 100\n    ch1.send 1\n  discard <| IO.asTask (prio := .dedicated) do\n    IO.sleep 50\n    ch2.send 2\n  (recv2 ch1 ch2).block\nsome 2\n\n\nBoth Selectable.one and Selectable.tryOne throw an exception when passed an empty array of selectables, because it's impossible to get a value from nothing.\n\nEvent selection is fair.\nThis means that there is an equal probability that any of the selectables with currently-resolved selectors win and have their associated code invoked.\nThis is important because a bias in event selection can lead to one of the selectables never being called, which can in turn cause data to accumulate without bound in the source it would have handled.\nBehind the scenes, fairness is ensured by randomizing the order of selectables each time.\n\n\n\nFurthermore, event selection never results in data being lost in the losing selectables.\nThe implementation ensures that data is never removed from a selector without being passed to the selectable's code, and that resolving a selector calls the associated selectable's code at most once.\nData loss and double delivery are ruled out via a protocol that distinguishes checking whether a selector is resolved from actually consuming its data along with an atomic means of selecting one of the resolved selectors.\n\n\n\n\n\n","context":"Lean Reference\u0009IO\u0009Asynchronous Programming","header":"21.12.3. Event Selection","id":"/IO/Async/#async-select"}});